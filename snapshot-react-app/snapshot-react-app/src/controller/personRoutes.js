// routes for person
const Person = (routeAction) => {
  switch (routeAction) {
    case 'create':
      // fetch(`/herokuurl/${routeAction}`).then(res => res.json)
      console.log('hit create route');
    default:
      return
  }
}

export default Person;
